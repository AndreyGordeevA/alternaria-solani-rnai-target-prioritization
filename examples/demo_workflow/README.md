# Demo workflow

This small dataset checks that the repository scripts run with generic candidate IDs and configurable toy references.

Run:

```bash
bash run_demo.sh
```

Expected result: the script creates `outputs/`, completes the 21-nt window check, candidate uniqueness BLASTn step and toy host-reference screen, and prints `Demo workflow completed`.
